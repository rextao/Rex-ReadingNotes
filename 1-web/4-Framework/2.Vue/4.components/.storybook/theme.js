import { create } from '@storybook/theming';
import logo from '../src/asset/img/polaris-logo.png';

export default create({
    base: 'light',
    brandTitle: 'polaris oversea',
    brandUrl: '',
    brandImage: logo,
});
