import { Meta, Story, ArgsTable,Canvas } from '@storybook/addon-docs';
import {
    CoCascader,
} from '@src/components/base/index';

<Meta
    title="Base/co-cascader"
    component={ CoCascader }
    argTypes={{
    }}
    args={{
        options: [
            {
                value: 1,
                label: '指南',
                children: [{
                    value: 11,
                    label: '设计原则设计原则设计原则设计原则'
                }]
            },
            {
                value: 2,
                label: '指南1',
                children: [{
                    value: 21,
                    label: '设计原则1',
                    children: []
                }]
            },
            {
                value: 3,
                label: '指南2',
                children: []
            },
        ],
        model: [1,11],
    }}
/>

export const Template = (args, { argTypes }) => ({
    components: {
        CoCascader,
    },
    props: Object.keys(argTypes),
    template: `
        <co-cascader v-bind="$props" v-model="model"></co-cascader>
    `,
});

## 代码引用
```js
import {
    CoCascader,
} from '@src/component/base/index';
```

### 隐藏单选时的radio按钮
在设置：props:{ checkStrictly: true }时，产品有时需要将前面的 ⭕ 隐藏掉，且点整行可以触发，可以设置： `hideRadio = true`

`hideRadio = true` 默认在8个字符长度，以上会显示tooltip
<Canvas>
    <Story
        name="隐藏单选时的radio按钮"
        args={{
            hideRadio: true,
            props:{ checkStrictly: true }
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>


### 去除最后的无数据面板
一般后端返回的树状数据是递归的，children最后无数据会返回空数组，cascader会渲染一个空数据看板
<Canvas>
    <Story
        name="去除最后的无数据面板"
        args={{
            setNull: true,
            model: [2,21],
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={CoCascader} />
