import { Meta, Story, ArgsTable, } from '@storybook/addon-docs';
import {
    CoSelect,
} from '@src/components/base/index.ts';

<Meta
    title="Base/co-select"
    component={ CoSelect }
    argTypes={{
    }}
    args={{
        model: 4,
    }}
/>

export const Template = (args, { argTypes }) => ({
    components: {
        CoSelect,
    },
    props: Object.keys(argTypes),
    template: `
        <co-select v-bind="$props" v-model="model"></co-select>
    `,
});

## 代码引用
```js
import {
    CoSelect,
} from '@src/component/base/index';
```

### select的简洁用法

如`options`返回字段非label与value，可以通过配置`label`和`val`解决

<Story
    name="select的简洁用法"
    args={{
        options: [
            { label: '上海', value: 1 },
            { label: '北京', value: 2 },
            { label: '广州', value: 3 },
            { label: '深圳', value: 4 },
        ],
    }}
>
    {Template.bind({})}
</Story>

```html
<co-select
    :options="[
        { label: '上海', value: 1 },
        { label: '北京', value: 2 },
        { label: '广州', value: 3 },
        { label: '深圳', value: 4 },
    ]"
></co-select>

<co-select
    val="id"
    label="text"
    :options="[
        { text: '上海', id: 1 },
        { text: '北京', id: 2 },
        { text: '广州', id: 3 },
        { text: '深圳', id: 4 },
    ]"
></co-select>
```

### 禁用某项

options的disabled字段，可以直接禁用当前项

<Story
    name="禁用某项"
    args={{
        options: [
            { label: '上海', value: 1, disabled: true },
            { label: '北京', value: 2, disabled: true },
            { label: '广州', value: 3 },
            { label: '深圳', value: 4 },
        ],
    }}
>
    {Template.bind({})}
</Story>

```html
<co-select
    :options="[
        { label: '上海', value: 1, disabled: true },
        { label: '北京', value: 2, disabled: true },
        { label: '广州', value: 3 },
        { label: '深圳', value: 4 },
    ]"
></co-select>
```

## slots

export const TemplateSlot = (args, { argTypes }) => ({
    components: {
        CoSelect,
    },
    props: Object.keys(argTypes),
    template: `
        <co-select v-bind="$props" v-model="model">
            <template #after-option>
                我是option最后的按钮！！！
            </template>
        </co-select>
    `,
});


### after-option

<Story
    name="after-option"
    args={{
        options: [
            { label: '上海', value: 1, },
            { label: '北京', value: 2, },
            { label: '广州', value: 3 },
            { label: '深圳', value: 4 },
        ],
        afterOption: 'after-option'
    }}
>
    {TemplateSlot.bind({})}
</Story>

```html
<co-select
    :options="[
        { label: '上海', value: 1, disabled: true },
        { label: '北京', value: 2, disabled: true },
        { label: '广州', value: 3 },
        { label: '深圳', value: 4 },
    ]"
>
    <template #after-option>
        我是option最后的按钮！！！
    </template>
</co-select>
```

<ArgsTable of={CoSelect} />
